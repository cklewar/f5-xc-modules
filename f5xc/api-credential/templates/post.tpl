{
   "expiration_days": "{{ vars.expiration_days }}",
   "name": "{{ vars.name }}",
   "namespace": "{{ vars.namespace }}",
   "spec": {
      "password": "{{ vars.password }}",
      "type": "{{ vars.type }}",
      "virtual_k8s_name": "{{ vars.virtual_k8s_name }}",
      "virtual_k8s_namespace": "{{ vars.virtual_k8s_namespace }}"
   }
}